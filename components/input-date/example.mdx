import InputDate from './src/input-date'

# InputDate

<Documenter component={InputDate}>

```jsx preview
const [value, setValue] = useState();

<InputDate value={value} onChange={setValue} />
```

</Documenter>

## Examples

- [Date input](#date-input)
- [Date input with time](#date-input-with-time)
- [Date range input](#date-range-input)
- [Date range input with time](#date-range-input-with-time)
- [Date range input with presets](#date-range-input-preset)
- [InputDate.Picker component](#inputdatepicker)
- [Presets component](#inputdatepicker) 

### Date input

```jsx preview
const [value, setValue] = useState();

<InputDate value={value} onChange={setValue} />
```

### Date input with time

```jsx preview
const [value, setValue] = useState();

<InputDate value={value} onChange={setValue} includeTime />
```

### Date range input

```jsx preview
const [value, setValue] = useState();

<InputDate value={value} onChange={setValue} range active="from" />
```

### Date range input with time

```jsx preview
const [value, setValue] = useState();

<InputDate value={value} onChange={setValue} range active="from"  includeTime/>
```

### Date range input preset

```jsx preview
const [value, setValue] = useState();

<InputDate value={value} onChange={setValue} range presets />
```

#### Presets

Presets can be manually added also. Below format should be used

```
{
  range: boolean
  label: string
  value(): Date | { from: Date; to: Date }
}
```

## Sub-components

### InputDate.Picker

<Documenter component={InputDate.Picker} sub>

```jsx preview
const [value, setValue] = useState();

<InputDate.Picker value={value} onChange={setValue} />
```

</Documenter>