
<br />

<Documenter>

```preview

const [val, setVal] = React.useState()
const [val2, setVal2] = React.useState()

const label = {
  from: 'Start Date 1',
  to: 'End Date 1'
}

const label2 = "Enter Date Here"

return (
  <>
    <InputDate range value={val} onChange={setVal} label={label} />
    <InputDate label={label2} value={val2} onChange={setVal2} includeTime disabledTime/>
  </>
)
```

</Documenter>
