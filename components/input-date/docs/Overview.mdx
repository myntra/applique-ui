
<br />

<Documenter>

```preview

const [val, setVal] = React.useState()
const [val2, setVal2] = React.useState()
return (
  <>
    <InputDate range={true} value={val} onChange={setVal} />
    <InputDate value={val2} onChange={setVal2} />
  </>
)
```

</Documenter>
