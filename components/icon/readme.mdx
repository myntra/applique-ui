import Icon from './src/icon'
import {Bell} from "@myntra/uikit-icons"

# Icon

<Documenter component={Icon}>

```jsx preview
<Icon name={Bell}/>
```

</Documenter>

## Icon size

```jsx preview
<div style={{
  display: 'flex',
  justifyContent: 'space-around',
  alignItems: 'center',
  flexDirection: 'column',
  height: '200px'
}}>
  <Icon name={BoxSolid} fontSize="small" />
  <Icon name={BoxSolid} fontSize="medium"/>
  <Icon name={BoxSolid} fontSize="large"/>
</div>
```

## Icon color

```jsx preview
<div style={{
  display: 'flex',
  justifyContent: 'space-around'
}}>
  <Icon name={BoxSolid}/>
  <Icon name={BoxSolid} fontSize="light"/>
  <Icon name={BoxSolid} color="primary"/>
  <Icon name={BoxSolid} fontSize="success"/>
  <Icon name={BoxSolid} fontSize="warning"/>
  <Icon name={BoxSolid} fontSize="error"/>
  <Icon name={BoxSolid} fontSize="disabled"/>
</div>
```

## Spinning Icons

A icon can be set to spin indefinitely. This can be used to create inline loaders.

```jsx preview
<Icon name="sync" spin />
```
