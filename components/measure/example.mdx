import Measure from './src/measure'

# Measure

<Documenter component={Measure}>

```jsx preview
const [content, setContent] = useState({});

<>
  <Measure bounds onMeasure={content => setContent(content)}>
    <textarea />
  </Measure>

  <pre>{JSON.stringify(content, null, 2)}</pre>
</>
```

</Documenter>
