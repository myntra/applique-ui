
<br />

<Documenter>

```preview
const [content, setContent] = React.useState({});

return (
  <>
    <Measure bounds onMeasure={content => setContent(content)}>
      <textarea />
    </Measure>

    <pre>{window.JSON.stringify(content, null, 2)}</pre>
  </>
)
```

</Documenter>
