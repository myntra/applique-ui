<Documenter >

<br />

```preview
return (
  <Badge>10</Badge>
)
```

</Documenter>

## Sizes
```preview
return (
  <div style={{
    display: 'flex',
    justifyContent: 'space-around',
    alignItems: 'center',
    flexDirection: 'column',
    height: '200px'
  }}>
    <Badge type="info" size="small">Small</Badge>
    <Badge type="info" size="regular">Regular</Badge>
  </div>
)
```

## Variant

### Solid

```preview
return (
  <div style={{
    display: 'flex',
    justifyContent: 'space-around'
  }}>
    <Badge type="error">error</Badge>
    <Badge type="warning">warning</Badge>
    <Badge type="success">success</Badge>
    <Badge type="incomplete">incomplete</Badge>
    <Badge type="info">info</Badge>
  </div>
)
```
### DISMISSABLE
```jsx preview
function Example() {
  const [isVisible, setVisible] = React.useState(true);

  return isVisible &&
    <Badge type="warning" onClose={() => setVisible(false)} icon={CheckSolid}>
      warning
    </Badge>
}
```

### Outlined

```jsx preview
return (
  <div style={{
    display: 'flex',
    justifyContent: 'space-around',
    alignItems: 'center'
  }}>
    <Badge type="error" variant="outlined">error</Badge>
    <Badge type="warning" variant="outlined">warning</Badge>
    <Badge type="success" variant="outlined">success</Badge>
    <Badge type="incomplete" variant="outlined">incomplete</Badge>
    <Badge type="info" variant="outlined">info</Badge>
  </div>
)
```

### Button With Icon
```jsx preview
function Example() {
  const [isVisible, setVisible] = React.useState(true);

  return(
    <div style={{
    display: 'flex',
    justifyContent: 'space-around',
    alignItems: 'center'
  }}>
    <Badge type="error" icon={CheckSolid}>error</Badge>
    <Badge type="warning" variant="outlined" icon={InfoCircleSolid}>warning</Badge>
    <Badge type="success" onClose={() => setVisible(false)} icon={CheckSolid}>success</Badge>
    <Badge type="info" variant="outlined" onClose={() => setVisible(false)} icon={InfoCircleSolid}>info</Badge>
    </div>
  )
  
}
```

