import InputMasked from './src/input-masked'

# InputMasked

<Documenter component={InputMasked}>

```jsx preview
const [value, setValue] = useState(null);

<InputMasked pattern={`dd"/"dd`} value={value} onChange={setValue} />
```

</Documenter>
