
<br />

<Documenter>

```preview
const [val, setVal] = React.useState()

return (
  <>
    <InputMasked includeMaskChars pattern={'LLLLLLLLLL'} value={val} onChange={value => {console.log(value); setVal(value)}} />
  </>
)
```

</Documenter>
