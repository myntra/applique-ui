import Pagination from './src/pagination'

# Pagination

<Documenter component={Pagination}>

```jsx preview
<Pagination />
```

</Documenter>

### Using onChange and setting values

```jsx preview
const [total] = useState(200)
const [size, setSize] = useState(10)
const [page, setPage] = useState(1)
<Pagination total={200} size={size} page={page} onChange={({page, size}) => { console.log("heyo", page, size); setSize(size); setPage(page) }}/>
```