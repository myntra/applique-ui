import InputSelect from './src/input-select'
import { Bell } from "uikit-icons"

# InputSelect

<Documenter component={InputSelect}>

```jsx preview
const [value, setValue] = useState();
const [options] = useState([
  { value: 'Red', label: 'Red' },
  { value: 'Green', label: 'Green' },
  { value: 'Blue', label: 'Blue' },
]);

<>
  <p><b>Bordered Variant</b></p>
  <br/>
  {'Choose your color: '}
  <InputSelect
    options={options}
    value={value} onChange={setValue}
  />
  <p><b>Standard Variant</b></p>
  <br/>
    {'Choose your color: '}
  <InputSelect
    options={options}
    value={value} onChange={setValue}
    variant="standard"
  />
</>
```

</Documenter>

## Error State 
```jsx preview
const [value, setValue] = useState();
const [options] = useState([
  { value: 'Red', label: 'Red' },
  { value: 'Green', label: 'Green' },
  { value: 'Blue', label: 'Blue' },
]);

<>
<p><b>Bordered Variant</b></p>
  {'Choose your colors: '}
  <InputSelect
    multiple
    options={options}
    value={value} onChange={setValue}
    error={true}
  />
  <p><b>Standard Variant</b></p>
   {'Choose your colors: '}
  <InputSelect
    multiple
    options={options}
    value={value} onChange={setValue}
    error={true}
    variant="standard"
  />
</>
```

## Multiple Selection

```jsx preview
const [value, setValue] = useState();
const [options] = useState([
  { value: 'Red', label: 'Red' },
  { value: 'Green', label: 'Green' },
  { value: 'Blue', label: 'Blue' },
]);

<>
  <p><b>Bordered Variant</b></p>
  {'Choose your colors: '}
  <InputSelect
    multiple
    options={options}
    value={value} onChange={setValue}
  />
  <p><b>Standard Variant</b></p>
  {'Choose your colors: '}
  <InputSelect
    multiple
    options={options}
    value={value} onChange={setValue}
    variant="standard"
  />
</>
```

## With Icon

```jsx preview
const [value, setValue] = useState();
const [options] = useState([
  { value: 'Red', label: 'Red' },
  { value: 'Green', label: 'Green' },
  { value: 'Blue', label: 'Blue' },
]);

<>
  <p><b>Bordered </b></p>
  {'Choose your colors: '}
  <InputSelect
    icon={Bell}
    options={options}
    value={value} onChange={setValue}
  />
  <p><b>Standard </b></p>
   {'Choose your colors: '}
  <InputSelect
    icon={Bell}
    options={options}
    value={value} onChange={setValue}
    variant="standard"
  />
</>
```
## With Adornment

```jsx preview
const [value, setValue] = useState();
const [options] = useState([
  { value: 'Red', label: 'Red' },
  { value: 'Green', label: 'Green' },
  { value: 'Blue', label: 'Blue' },
]);

<>
  <p><b>Bordered </b></p>
  {'Choose your colors: '}
  <InputSelect
    icon={Bell}
    options={options}
    value={value} onChange={setValue}
    adornment={<p>/kg</p>}
  />
  <p><b>Standard </b></p>
   {'Choose your colors: '}
  <InputSelect
    icon={Bell}
    options={options}
    value={value} onChange={setValue}
    variant="standard"
    adornment={<p>/kg</p>}
  />
   <p><b>AdornmentPosition Start </b></p>
   {'Choose your colors: '}
  <InputSelect
    icon={Bell}
    options={options}
    value={value} onChange={setValue}
    adornment={<p>/kg</p>}
    adornmentPosition="start"
  />
</>
```


## Loading options from API

```jsx preview
const [countries, setCountries] = useState([]);
const [value, setValue] = useState(null);

useEffect(() => {
  fetch(`https://restcountries.eu/rest/v2/all?t=${Date.now()}`)
    .then(response => response.json())
    .then(data => data.map(country => ({ value: country.alpha2Code, label: country.name })))
    .then(countries => setCountries(countries))
}, []);

<>
  {'Where you wanna go? '}
  <InputSelect
    isLoading={countries.length === 0}
    options={countries}
    value={value} onChange={setValue}
    multiple
  />
</>
```

## Readonly state

```jsx preview
const [value, setValue] = useState();
const [options] = useState([
  { value: 'Red', label: 'Red' },
  { value: 'Green', label: 'Green' },
  { value: 'Blue', label: 'Blue' },
]);

<>
  {'Choose your color: '}
  <InputSelect
    options={options}
    value={'Green'} onChange={setValue}
    readOnly
  />
</>
```

## Disabled state

```jsx preview
const [value, setValue] = useState();
const [options] = useState([
  { value: 'Red', label: 'Red' },
  { value: 'Green', label: 'Green' },
  { value: 'Blue', label: 'Blue' },
]);

<>
  {'Choose your color: '}
  <InputSelect
    options={options}
    value={value} onChange={setValue}
    disabled={true}
  />
  <p><b>Standard Variant</b></p>
    {'Choose your color: '}
  <InputSelect
    options={options}
    value={value} onChange={setValue}
    disabled={true}
    variant="standard"
  />
</>
```

## Disabled With a Value

```jsx preview
const [value, setValue] = useState();
const [options] = useState([
  { value: 'Red', label: 'Red' },
  { value: 'Green', label: 'Green' },
  { value: 'Blue', label: 'Blue' },
]);

<>
  {'Choose your colors: '}
  <InputSelect
    disabled
    options={options}
    value={'Green'} onChange={setValue}
  />
</>
```
