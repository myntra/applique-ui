  
This section contains components attributes, anatomy and variation of component type.

<Documenter component={props.components}>

```
return (
<>
<Grid multiline={true} centered gap="large">
    <Grid.Column size="one-quarter">
      <Button type="primary" icon={CheckCircle}>Button Label</Button>
    </Grid.Column>
    <Grid.Column size="one-quarter">
      <Button type="secondary" icon={CheckCircle}>Button Label</Button>
    </Grid.Column>
    <Grid.Column size="one-quarter">
      <Button type="tertiary" icon={CheckCircle}>Button Label</Button>
    </Grid.Column>
    <Grid.Column size="one-quarter">
      <Button type="warning" icon={CheckCircle}>Button Label</Button>
    </Grid.Column>
    <Grid.Column size="one-quarter">
      <Button type="primary" icon={EllipsisVSolid}/>
    </Grid.Column>
    <Grid.Column size="one-quarter">
      <Button type="secondary" icon={EllipsisVSolid}/>
    </Grid.Column>
    <Grid.Column size="one-quarter">
      <Button type="tertiary" icon={EllipsisVSolid}/>
    </Grid.Column>
    <Grid.Column size="one-quarter">
      <Button type="warning" icon={EllipsisVSolid}/>
    </Grid.Column>
</Grid>
</>
)
```

</Documenter>

<small>Filled , Outlined, No-outlined, Warning and Icon only button</small>

The Button component has a lot of variations we can use. It contains a label and optional icons before or after the label. 

Fill, size, and width can all be mixed and matched to get the exact button needed.

## States

States referes to the different forms an element can take based on context that it is being used in or it can be said - its a visual representation to communicate the status of component or interactive element.   


<Documenter component={props.components}>

```
return (
  <Grid multiline={true} centered gap="large">
    <Grid.Column size="one-fifth">
      <div></div>
    </Grid.Column>
    <Grid.Column size="one-fifth">
      Primary
    </Grid.Column>
    <Grid.Column size="one-fifth">
      Secondary
    </Grid.Column>
    <Grid.Column size="one-fifth">
      Tertiary
    </Grid.Column>
    <Grid.Column size="one-fifth">
      Warning
    </Grid.Column>

    <Grid.Column size="one-fifth">
      Default
    </Grid.Column>
    <Grid.Column size="one-fifth">
      <Button type="primary">Button Label</Button>
    </Grid.Column>
    <Grid.Column size="one-fifth">
      <Button type="secondary">Button Label</Button>
    </Grid.Column>
    <Grid.Column size="one-fifth">
      <Button type="tertiary">Button Label</Button>
    </Grid.Column>
    <Grid.Column size="one-fifth">
      <Button type="warning">Button Label</Button>
    </Grid.Column>

    <Grid.Column size="one-fifth">
      Hover
    </Grid.Column>
    <Grid.Column size="one-fifth">
      <Button type="primary">Button Label</Button>
    </Grid.Column>
    <Grid.Column size="one-fifth">
      <Button type="secondary">Button Label</Button>
    </Grid.Column>
    <Grid.Column size="one-fifth">
      <Button type="tertiary">Button Label</Button>
    </Grid.Column>
    <Grid.Column size="one-fifth">
      <Button type="warning">Button Label</Button>
    </Grid.Column>

    <Grid.Column size="one-fifth">
      Clicked
    </Grid.Column>
    <Grid.Column size="one-fifth">
      <Button type="primary">Button Label</Button>
    </Grid.Column>
    <Grid.Column size="one-fifth">
      <Button type="secondary">Button Label</Button>
    </Grid.Column>
    <Grid.Column size="one-fifth">
      <Button type="tertiary">Button Label</Button>
    </Grid.Column>
    <Grid.Column size="one-fifth">
      <Button type="warning">Button Label</Button>
    </Grid.Column>

    <Grid.Column size="one-fifth">
      Disabled
    </Grid.Column>
    <Grid.Column size="one-fifth">
      <Button type="primary" disabled>Button Label</Button>
    </Grid.Column>
    <Grid.Column size="one-fifth">
      <Button type="secondary" disabled>Button Label</Button>
    </Grid.Column>
    <Grid.Column size="one-fifth">
      <Button type="tertiary" disabled>Button Label</Button>
    </Grid.Column>
    <Grid.Column size="one-fifth">
      <Button type="warning" disabled>Button Label</Button>
    </Grid.Column>

    <Grid.Column size="one-fifth">
      Loading
    </Grid.Column>
    <Grid.Column size="one-fifth">
      <Button type="primary" loading>Button Label</Button>
    </Grid.Column>
    <Grid.Column size="one-fifth">
      <Button type="secondary" loading>Button Label</Button>
    </Grid.Column>
    <Grid.Column size="one-fifth">
      <Button type="tertiary" loading>Button Label</Button>
    </Grid.Column>
    <Grid.Column size="one-fifth">
      <Button type="warning" loading>Button Label</Button>
    </Grid.Column>
  </Grid>
)
```

</Documenter>

# Variants
## Primary Button

To call attention to an action on a form, or highlight the strongest call to action on a page. Primary buttons should only appear once per screen.

**Example : Save, Confirm, Done**

**Sequence: Default > Hover > clicked > Disabled**

<Documenter component={props.components}>

```preview
return (
  <Button type="primary">Button Label</Button>
)

```

</Documenter>

## Secondary Button

For Secondary actions on the page. Use against shaded or colorful backgrounds. An Secondary button will maintain the appropriate visual weight and won’t clash with the background color and maintain the visual hierarchy. 

**Example:**

**Sequence: Default > Hover > clicked > Disabled**

<Documenter component={props.components}>

```preview
return (
  <Button type="secondary">Button Label</Button>
)

```

</Documenter>

## Tertiary Button

For least insignificant actions on the page. They can be paired with primary and secondary button when there are multiple calls to action. 

**Example:**

**Sequence: Default > Hover > clicked > Disabled**

<Documenter component={props.components}>

```preview
return (
  <Button type="tertiary">Button Label</Button>
)

```

</Documenter>

## Warning Button

Warning button is used when user is asked to take an action, usually during the final confirmation for a destructive or negative action. They are generally used mostly in confirmation modals. 

**Example :  Delete**

**Sequence: Default > Hover > clicked**

<Documenter component={props.components}>

```preview
return (
  <Button type="warning">Button Label</Button>
)

```

</Documenter>


## Icon only Button

When pairing an Icon and an action, an Icon-only Button is used to control the interaction. To add clarity to the action, Button Icons should be paired with Tooltips

**Example :  More**

**Sequence: Default > Hover > clicked**

<Documenter component={props.components}>

```preview
return (
  <Button type="warning">Button Label</Button>
)

```

</Documenter>
